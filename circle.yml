machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t ryanawilson/pluralsight-docker-ci

deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
#	  - docker push Ryan-A-Wilson/pluralsight-docker-ci.git

notify:
  webhooks:
    - url: https://registry.hub.docker.com/u/ryanawilson/pluralsight-docker-ci/trigger/f3b120ec-d9e1-40f7-ad43-4475885d3519/

test:
  override:    
    - mocha